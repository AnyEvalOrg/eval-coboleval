# AnyEval chart default values; provider chart is an explicit exception.
# AnyEval: set INSPECT_K8S_DEFAULT_NAMESPACE=anyeval-sandbox.
global:
  nameOverride: ""
  fullnameOverride: ""
automountServiceAccountToken: false
serviceAccountCreate: false
allowDomains: []
allowEntities: []
allowCIDR: []
services:
  default:
    # Cloud Build tag matches pyproject.toml; AnyEval pins the digest in its catalog.
    image: us-central1-docker.pkg.dev/openevalz-sbx-84737/openevalz/eval-cobol-sandbox:1.0.0
    command: ["sleep", "infinity"]
    workingDir: /tmp
    runtimeClassName: gvisor
    nodeSelector:
      cloud.google.com/gke-spot: "true"
    networkIsolated: true
    securityContext:
      runAsUser: 0
      runAsGroup: 0
      runAsNonRoot: false
      allowPrivilegeEscalation: false
      readOnlyRootFilesystem: true
      capabilities:
        drop: ["ALL"]
        add: ["SETUID", "SETGID", "KILL", "CHOWN", "DAC_OVERRIDE"]
      seccompProfile:
        type: RuntimeDefault
    volumeMounts:
      - name: tmp
        mountPath: /tmp
      - name: shm
        mountPath: /dev/shm
    volumes:
      - name: tmp
        emptyDir:
          sizeLimit: 512Mi
      - name: shm
        emptyDir:
          medium: Memory
          sizeLimit: 16Mi
    # Equal CPU/memory requests and limits provide Guaranteed QoS.
    # Budget: 768 MiB aggregate candidate RSS watchdog + at most one 50 ms
    # allocation burst + /dev/shm + supervisor must fit below 2 GiB.
    # Native RLIMIT_AS/DATA=1 GiB supplements the aggregate RSS watchdog.
    # gVisor records oom_score_adj but does not use it for host sandbox OOM;
    # the watchdog, not OOM victim preference, protects the supervisor.
    # Pod ephemeral-storage: 1 GiB; disk-backed /tmp emptyDir sizeLimit: 512 MiB
    # is an eviction backstop, not a synchronous filesystem limit. gVisor does
    # not enforce memory emptyDir sizeLimit as a tmpfs size. The disk watchdog
    # sums allocated blocks across ALL of /tmp and /dev/shm: 256 MiB + one
    # 100 ms write burst; gVisor gofer write throughput makes that burst small.
    # /dev/shm retains medium Memory / sizeLimit 16 MiB; the watchdog covers it
    # too rather than trusting that sizeLimit. Root filesystem stays read-only.
    # RLIMIT_NPROC=64 reserves slots below the reference Docker pids limit (128).
    resources:
      requests:
        cpu: "1"
        memory: 2Gi
        # Candidate files/captures now live in disk-backed /tmp.
        ephemeral-storage: 1Gi
      limits:
        cpu: "1"
        memory: 2Gi
        ephemeral-storage: 1Gi
# Standard NetworkPolicy is visible to AnyEval's provenance hook (which queries
# networking.k8s.io). The Autopilot cluster has no CiliumNetworkPolicy CRD;
# the custom AnyEval chart emits only this standard policy, including DNS denial.
additionalResources:
  - apiVersion: networking.k8s.io/v1
    kind: NetworkPolicy
    metadata:
      name: '{{ .Release.Name }}-coboleval-deny-all'
      namespace: '{{ .Release.Namespace }}'
    spec:
      # Select THIS release's pod only. `podSelector: {}` selects every pod in the namespace,
      # including shared infrastructure such as the egress proxy, and starved it of egress.
      podSelector:
        matchLabels:
          app.kubernetes.io/instance: '{{ .Release.Name }}'
      policyTypes: [Ingress, Egress]
      ingress: []
      egress: []
